# Base path for the DockIQ API. In dev, Vite proxies /api to localhost:8080
# (see vite.config.ts) so the default of "/api/v1" works out of the box.
# Set this only if the API is served from a different origin/path.
VITE_API_BASE=/api/v1
